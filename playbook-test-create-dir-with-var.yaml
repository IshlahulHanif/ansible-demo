---
# playbooks file for golang install

- name: Test Create dirs
  hosts: server-a
  vars:
    default_nama_tim: astro
  tasks:
    - name: create dir
      file:
        path: '/usr/{{ nama_tim | default(default_nama_tim) }}'
        state: directory
        owner: root
        group: root
        mode: 0644